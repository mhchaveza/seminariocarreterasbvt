{
  "name": "site",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "clean": "rm -rf .astro dist node_modules/.astro",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": ". ~/.nvm/nvm.sh && nvm use 22 && npm run clean && astro build && firebase deploy --only hosting:seminariocarreterasbvt"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.2",
    "@tailwindcss/vite": "^4.3.0",
    "astro": "^6.3.1",
    "tailwindcss": "^4.3.0"
  },
  "devDependencies": {
    "playwright": "^1.59.1"
  }
}
